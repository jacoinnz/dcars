{
  "name": "dcars",
  "private": true,
  "description": "Helper scripts from repo root. The Next.js app is only under ./web — set Vercel Root Directory to web.",
  "scripts": {
    "build": "npm run build --prefix web",
    "dev": "npm run dev --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
