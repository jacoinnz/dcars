{
  "name": "dcars",
  "private": true,
  "description": "Monorepo root: the Next.js app is the npm workspace package `web`. Root install must run so `next` exists when Vercel builds from the repository root.",
  "workspaces": ["web"],
  "scripts": {
    "build": "npm run build -w web",
    "dev": "npm run dev -w web",
    "start": "npm run start -w web",
    "lint": "npm run lint -w web"
  }
}
